apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-test-helm.fullname" . }}
  labels:
    {{- include "devops-test-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-test-helm.selectorLabels" . | nindent 4 }}
