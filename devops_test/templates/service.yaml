apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops_test.fullname" . }}
  labels:
    {{- include "devops_test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops_test.selectorLabels" . | nindent 4 }}
